# ─── 核心框架 ───
fastapi==0.104.1
uvicorn[standard]==0.24.0.post1
sqlalchemy>=2.0.49
alembic==1.12.1
pydantic>=2.4.2
pydantic-core>=2.11.0
python-dotenv==1.0.0
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
httpx==0.25.2
Pillow>=12.0.0
python-multipart==0.0.6
PyYAML>=6.0

# ─── 新增依赖（P2/P3） ───
redis>=5.0.0          # 缓存层（可选，功能降级无此依赖也可运行）
psycopg2-binary>=2.9.9  # PostgreSQL 驱动（可选，默认 SQLite）
asyncpg>=0.29.0        # 异步 PostgreSQL 驱动（可选）
celery>=5.3.0          # 任务队列（可选）
flower>=2.0.0          # Celery 监控 UI（可选）
